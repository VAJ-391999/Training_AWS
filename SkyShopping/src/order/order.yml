createOrder:
  handler: src/order/functions/createOrder.handler
  events:
    - http:
        path: /order/create
        method: POST
        cors: true
        authorizer: ${self:custom.authorizer.users}